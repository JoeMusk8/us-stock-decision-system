# 本地数据结构说明

`data/` 目录用于未来保存本地数据结构。

当前只有示例结构，没有真实数据。
`evidence_items.example.json` 不能当作真实证据。
所有真实事实必须绑定 `evidence_id`。
没有 `evidence_id` 的 Claim 不能进入事实区。
社交媒体 / KOL 数据只能作为线索。

当前阶段禁止事项：

* 不接 API。
* 不写爬虫。
* 不生成真实行情数据。
* 不生成真实财务数据。
* 不生成真实宏观数据。
* 不生成真实公司结论。
* 不写真实 URL、真实公司名或真实股票代码。
